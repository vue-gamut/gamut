{
  "name": "gamut",
  "version": "1.0.0",
  "description": "Vue.js component library monorepo",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish",
    "release": "pnpm build && pnpm changeset:publish"
  },
  "keywords": [
    "vue",
    "monorepo",
    "components"
  ],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0",
  "devDependencies": {
    "@changesets/cli": "2.29.4"
  }
}
